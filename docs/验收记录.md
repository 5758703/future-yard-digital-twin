# 验收记录

日期：2026-09-23。环境：Windows、Node.js 24.15、Vue 3、Three.js、Vite。数据均为模拟。

## 自动化验证
- `npm test`：空间 ID 与坐标边界、房间容量、设备关联、遥测控制、告警顺序与必填记录、疏散备用楼梯和容量、能耗空间作用域、暖通停机与告警设备读数。
- `npm run build`：生产资源构建成功；Three.js 主依赖约 551 kB（gzip 约 140 kB），Vite 给出常规体积提示。
- `npm run data:export`：6 栋、37 层、296 房间、2072 台房间设备；另含 6 个摄像头和 4 个室外设施。
- `npm run model:export`：生成内嵌资源的 GLB 2.0，约 0.39 MB，2066 个节点；模型保留 6 个 buildingId、37 个 floorId、296 个 roomId。
- `python -m py_compile blender/generate_campus.py blender/run_with_bpy.py`：语法检查。

## 浏览器实测
- 行政办公楼 → 3F → 301 室：空间树、三维剖切、房间面积与人数联动。
- 人才公寓一期 → 5F → 502 室：住宅床位模型、42 m²、5.75 × 7.30 × 3.3 m 与坐标展示。
- 房间灯光关闭后开关状态更新；刷新后控制仍保留。
- AL-001 告警完整经过诊断、派单、处理与复核；未填处理记录时阻止提交；闭环后的审计时间线仍可查看。
- 访客登记、模拟越界、返回授权区、签退成功。演示访客已签退。
- 疏散东侧楼梯路径与西侧备用路径切换成功，楼梯通行量从 156 降为 78 人/分钟。
- 设备台账按 A-301 搜索得到 7 台关联设备，并展示当前模拟读数。
- 水电曲线切换、负荷排行、对 72 个无人房间应用节能策略后模拟负荷下降。
- 后厨模拟监控、冷藏温度、燃气和油烟净化效率可查看。
- 390 × 844 窄屏检查无页面横向溢出，空间树入口可用，窄屏三维取景已适配。
- 检查浏览器运行日志未发现应用错误。

## 已知边界
- 当前提供可直接导入 Blender 的 GLB 与原生 Blender 生成脚本。由于本机没有 Blender，且运行库的大文件下载与依赖安装未完成，未在本机生成或验证 `.blend` 文件。没有把 Three.js 导出的 GLB 宣称为 Blender 导出结果。
- 监控画面为合成示意；天气、人员、设备、阈值和预测均为演示。
- 暖通预测使用可解释的固定规则，没有训练模型；疏散为几何/通行率估算，不能用于实际安全决策。
- 验收浏览器保留一个已闭环事件、已签退的演示访客与节能模拟控制，用于查看操作结果；其他浏览器初次打开使用初始模拟数据。

## 模型导入实测
- 已将 public/models/future-yard.glb 通过网页文件选择器导入成功。
- 导入后点击行政办公楼、进入 3F，仍可显示 301 等房间；恢复默认模型按钮已验证。
- 最终自动化测试共 7 项，全部通过。


## 通勤动画增补验收
- 16 辆车沿环路中心线两侧独立车道对向行驶，按路程计算位置、转弯朝向与车轮转动；停车位移至道路外侧。
- 24 个行人角色沿人行道进出 A/B 办公楼，包含摆臂、迈步、进出渐隐、楼内停留及感应门。角色池人数与实际可见人数不同，不修改业务人数。
- 上班、下班、双向模式，暂停/继续，1×/2×/4× 和入口近景均经浏览器操作验证。进入楼层隐藏通勤，返回保留原控制状态。
- 默认参数化模型和更新后的 GLB 均已检查入口近景；GLB 导入后仍有独立人行道、车流与人员动画。浏览器控制台无应用错误。
- 最终 `npm test`：12 项全部通过，覆盖双向车道间距、闭环连续性、路径不穿楼、进出/隐藏阶段、暂停/倍速/帧率、GLB 门洞净空及停车位位置。
- `npm run build` 成功；仅保留 Three.js 依赖包超过 500 kB 的常规体积提示。`git diff --check` 通过。
- Blender 门洞脚本通过 Python 语法检查；本机仍未执行 Blender 原生导出。运行时动画由 Three.js 驱动，未烘焙为 Blender/GLB 动画轨道。
